# L04 — Trees, Ensembles & Tuning — Setup

Environment setup is at the repo root: ➡ **[../SETUP.md](../SETUP.md)**

If you completed setup for an earlier lesson, your `dsai-m3` environment already has everything L04 needs. Skip ahead.

---

## What's new this lesson

**Dependencies:** `HistGradientBoostingClassifier`, `RandomForestClassifier`, `GridSearchCV` (already in `dsai-m3`)

**Data:** Same `northstar_churn.csv` as L03 + a banking-fraud generator built into the assignment.


## Sanity check

Open any notebook in this lesson, pick the `dsai-m3` kernel, run the setup cell. If anything errors, see **Troubleshooting** in [../SETUP.md](../SETUP.md).
